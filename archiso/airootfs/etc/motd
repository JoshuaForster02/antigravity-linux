
  FLYNN OS  ·  The Grid

  Desktop starts automatically on tty1.   Login: root / tron
  help → shell commands     Super+? → all shortcuts     flynn-setup → Steam/Anki
  flynn-update → OTA update              flynn-install → install to disk

