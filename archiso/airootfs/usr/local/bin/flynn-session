#!/bin/bash
# Flynn OS — legacy session launcher (RETIRED)
# The custom flynn-compositor experiment is over: Sway IS the Flynn OS session
# and starts automatically on tty1. This shim only points people there.
echo ""
echo "  flynn-session is retired — Sway is the Flynn OS session."
echo "  It starts automatically on tty1. From a text console, just log in"
echo "  on tty1 or run:  dbus-run-session sway --unsupported-gpu"
echo ""
exit 0
